// import { Requireable, Validator } from 'prop-types';
//
//
//
// declare module 'prop-types' {
//   export interface Requireable<T> extends Validator<T | undefined | null> {
//     isRemoteState(): Requireable<T>;
//     // isLocalhostState(): Requireable<T>;
//     // isCookieState(): Requireable<T>;
//   }
// }

// PropTypes.prototype.isRemoteState = () => { /*nothing here*/ };

export const ters = () => {
  /*no content*/
};
